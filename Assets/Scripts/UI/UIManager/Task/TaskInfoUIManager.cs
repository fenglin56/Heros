using UnityEngine;
using System.Collections;

namespace UI.MainUI
{

    public class TaskInfoUIManager : MonoBehaviour
    {
        /// <summary>
        /// 任务管理面板
        /// </summary>

        //const MainUIFirstPanelType MyPanelType = MainUIFirstPanelType.TaskInfo;

        void Start()
        {
            //transform.parent.GetComponent<MainUIController>().SetPanelActivEvent += new MainUIController.SetPanelDelegate(SetPanelEnable);
        }

        void SetPanelEnable(string[] UIStatus)
        {

        }
    }
}
